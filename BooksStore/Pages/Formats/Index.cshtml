@page
@model BooksStore.Pages.Formats.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Format[0].formatType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Format[0].dateAdded)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Format) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.formatType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dateAdded)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.formatID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.formatID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.formatID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
