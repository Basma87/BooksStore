@page
@model BooksStore.Pages.Audiences.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Audience[0].audienceName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Audience[0].dateAdded)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Audience) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.audienceName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dateAdded)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AudienceID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AudienceID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AudienceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
